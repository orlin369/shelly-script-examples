Regenerate SHELLY_MJS.md from examples-manifest.json using tools/sync-manifest-json.py.

Run this after editing examples-manifest.json or after /sync-manifest to keep the Markdown index in sync.

Usage: /gen-index

Run this command from the repo root:

```
python tools/sync-manifest-json.py $ARGUMENTS
```

Output: writes SHELLY_MJS.md next to examples-manifest.json in the repo root.
